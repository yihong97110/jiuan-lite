# jiuan-lite 训推平台 · 工作报告

日期：2026-07-15　｜　负责人：（本人）　｜　运行环境：Windows 10 + CPU，conda 环境 `jiuan`(py3.10)，模型 Qwen2.5-0.5B-Instruct

## 一、本阶段目标
对标"久安"训推平台做初级版，跑通「标·训·推·评」全链路，并在此基础上补齐数据驱动迭代、标注回灌、RAG 知识库三条闭环。本周期重点：修正 v4 数据泄漏、落地 RAG 检索增强并实现"缺口通知 + 一键补充"闭环。

## 二、本周期完成事项
1. 修正迭代对比的数据泄漏（根因级）
   - 问题：标注回灌把固定评测集里的 6 个 prompt 混入了 v4 训练集，导致 train/eval 交叉污染，v4 指标不可信。
   - 处理：registry 增加 held-out 评测集标记；`annotate.create_task/commit` 自动排除评测集 prompt（防"拿考题当练习题"）；删除泄漏的旧 v4，重建干净 v4 并重训重评。
2. RAG 知识库实跑演示（弥补 SFT 硬记不足）
   - 入库应急法规示例文档，做"无RAG vs 有RAG"对比：RAG 将 SFT 答错/编造的回答（AED、危化品）纠正为法规原文，检索命中分 0.38~0.52。
3. RAG 缺口闭环（本周期新增能力）
   - 检索最高命中分低于阈值(0.15)时自动判定"知识库缺内容"，记录缺口台账；⑥页顶部红色徽标通知；每条缺口"补充知识"按钮贴入权威条文后自动重新入库，下次提问即命中。
   - 端到端验证：燃气泄漏问题命中分 0.1483→补充后 0.5134，答案精确引用补充条文，缺口归零。

## 三、当前系统能力（7 个页面，全部本机 REAL 验证）
- ① 标·数据：清洗/去重/train-valid 切分 + 数据集血缘增量继承
- ② 训·训练：多后端(LLaMA-Factory/内置transformers/mock)，CPU/GPU 可选，页面可调超参，实时进度
- ③ 推·推理：transformers/vLLM/mock + token 计量 + 批量推理
- ④ 评·评测：ROUGE-L/BLEU + LLM-as-Judge(火山方舟) + 模型对比 + gap 薄弱点分析
- ⑤ 迭代：按血缘链展示 v1→vN 指标对比
- ⑥ RAG：离线 TF-IDF 检索增强 + 缺口通知 + 一键补充入库（本周期新增闭环）
- ⑦ 标注：gap→标注→回灌训练集的轻量闭环（已加防泄漏）

## 四、真实迭代链指标（同一 held-out 评测集 + 火山裁判，全 REAL）
| 版本 | 数据量 | 新增 | ROUGE-L | BLEU-1 | judge | 说明 |
|---|---|---|---|---|---|---|
| v1 | 29 | +29 | 0.2547 | 0.3533 | 3.31 | 基线：火灾+地震+急救 |
| v2 | 36 | +7 | 0.2751 | 0.3878 | 3.56 | 补危化品 |
| v3 | 44 | +8 | 0.2859 | 0.3917 | 3.44 | 补公共卫生+洪涝台风 |
| v4 | 47 | +3 | 0.2834 | 0.4028 | 3.44 | 无泄漏干净对比 |

- v1→v3 补数据带来 ROUGE/BLEU 稳步提升，验证闭环有效；v4 仅加 3 条干净样本、评测集严格 held-out，指标基本持平属正常小样本波动，口径干净可自圆其说。

## 五、结论与说明
- 全链路 + 三条闭环（数据迭代 / 标注回灌 / RAG）已在本机以真实 0.5B 模型跑通，指标真实且防泄漏。
- 与久安差距主要是数据规模（对方有标注团队持续产数据），本项目已用最小成本验证了闭环迭代的有效性，后续对接业务系统即可扩量。

## 六、后续规划
- 数据侧：针对 AED/急救等持续薄弱点补新领域 QA，做 v5 迭代。
- 推理侧：Linux+GPU 上接 vLLM 提升吞吐。
- RAG 侧：可选升级 bge 语义嵌入(JIUAN_RAG_EMBED=bge)，接入更多法规/预案文档。
- 部署侧：迁移到腾讯云服务器对外提供内网访问。
