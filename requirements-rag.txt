# RAG 知识库依赖
# 默认离线后端：TF-IDF(scikit-learn)，零下载，CPU 即用
scikit-learn>=1.3
numpy>=1.24

# 可选：语义嵌入升级（需联网下载 bge 模型，设 JIUAN_RAG_EMBED=bge 启用）
# sentence-transformers>=2.6
# 可选：换 Chroma 向量库后端
# chromadb>=0.5
