# 工作汇报：通用大模型训练 Agent 交付说明

汇报日期：2026-07-17

## 一句话总结

我们把原来只能跑“生物专家”的专用训练流程，升级成了一个可面向任意业务方向的通用训练 Agent。用户只需要描述“希望强化大模型的方向”，系统就能自动生成训练数据、验证集和知识库，并完成多轮标注回灌、训练、评测和广度分析。

## 本次交付

1. 新增 `⑨ 通用训练 Agent` 页面。
   - 用户可以输入自然语言目标，例如“训练合同审查模型”或“训练客服质检模型”。
   - Agent 自动生成训练方案和默认参数。
   - 页面解释关键参数含义，方便非技术人员理解和调整。

2. 新增通用训练 Agent 后端。
   - 自动生成训练 source。
   - 自动生成 held-out 验证集，避免训练数据泄漏到评测集。
   - 自动生成配套知识库，并接入 RAG collection。
   - 自动拆分多轮迭代，每轮完成标注回灌、训练、评测、广度分析。
   - 自动保存步骤报告，便于复盘和交接。

3. 保留原有生物专家流程。
   - `⑧ Agent` 中的生物三轮、一键保通仍保留。
   - 新增 9 号页解决“只能训练生物问题”的局限。

4. 支持自定义基底模型。
   - 默认使用本机 Qwen2.5-0.5B demo 模型。
   - 可改为其他本地模型目录或 HF 模型名称。
   - 页面说明了大模型和小模型的取舍。

5. 完成 smoke 验证。
   - 用“法律合同审查”作为新业务方向跑通 1 轮 demo。
   - 生成了 source、held-out、知识库、训练数据集、模型、评测报告、广度报告和步骤报告。

## 老板视角的价值

这次交付的核心价值不是“多做了一个页面”，而是把训练平台从“单一场景 demo”推进到了“通用场景生产雏形”。

- 对业务：不同部门可以输入自己的方向，快速生成一个可跑通的领域模型训练流程。
- 对产品：形成了从需求描述到模型训练的可视化闭环，降低了使用门槛。
- 对工程：训练、评测、知识库、标注回灌、步骤报告都复用现有平台能力，后续扩展不需要重写一套系统。
- 对交付：每次训练都会保存参数、步骤、产物和问题分析，方便复现、汇报和审计。

## 可演示流程

建议演示路径：

1. 打开 `http://127.0.0.1:8000/?v=custom9`
2. 进入 `⑨ 通用训练 Agent`
3. 输入一个业务方向：
   - 合同审查
   - 客服质检
   - 应急预案问答
   - 化学实验安全
4. 点击“让 Agent 生成方案”
5. 展示系统自动回填参数和参数说明
6. 点击“启动通用训练”
7. 展示实时进度：
   - 标数据阶段
   - 回灌阶段
   - 训练阶段
   - 评测阶段
   - 广度分析阶段
8. 展示最终产物：
   - 数据集 id
   - 模型 id
   - 知识库 collection
   - 评测指标
   - 步骤报告

## 当前验证结果

smoke 场景：法律合同审查

- 任务：`9a6261099902`
- 数据生成：template
- 训练后端：mock
- 样本数：6
- 验证样本：3
- 迭代轮数：1
- 知识库：`custom-smoke`，入库 8 块
- 数据集：`contract-v1-合同审查知识-20260717-164008`
- 模型：`contract-v1-合同审查专家-20260717-164012`
- 评测：ROUGE-L `0.1389`，BLEU-1 `0.2361`，BLEU-2 `0.0955`
- 广度报告：`breadth-contract-v1-_-20260717-164012-20260717-164134`

说明：本次 smoke 主要验证全流程可跑通，使用的是 mock 和极小样本，不代表正式模型效果。正式演示可切换为默认 20 条、3 轮、hf 后端。

## 已沉淀的工程能力

- 通用 Agent：`jiuan/workers/custom_agent.py`
- 通用 API：`/agent/custom/plan`、`/agent/custom/start`、`/agent/custom/status`
- 通用 UI：`⑨ 通用训练 Agent`
- 基底模型可配置：`base_model_path`
- 流程报告：`data/agent_steps/custom-*.md/json`
- 工作流文档：`data/agent_steps/custom-agent-workflow-20260717.md`

## 风险与边界

1. 自动生成数据适合 demo 和冷启动，不应替代真实业务数据。
2. 小模型训练速度快，但专业能力上限有限。
3. CPU 可跑通流程，但正式训练建议使用 GPU。
4. Judge 可提升生成和评测质量，但依赖外部模型服务稳定性。

## 下一步建议

1. 用 9 号页跑一次正式三轮 demo：20 条样本、3 轮、hf 后端。
2. 接入真实业务资料上传，自动抽取训练样本。
3. 将 impact 回灌效果验证接入每轮训练结束后，形成“回灌是否有效”的自动判断。
4. 增加模板库：合同审查、客服质检、应急问答、设备运维、医学科普等常见方向一键加载。

## 汇报结论

本阶段已经完成从“生物专用训练 demo”到“通用大模型训练 Agent”的关键升级。平台现在不仅能展示单场景能力，也具备了面向不同业务方向快速生成、训练、评测和复盘模型的通用闭环能力。下一阶段重点应转向真实业务数据接入和正式三轮训练验证。
